from my_assistant import mac

mappings = {
    # 'greeting'  : mac.hello,
    # 'set volume': mac.set_volume,
    # 'set rate'  : mac.set_rate,
    'set_name'  : mac.set_name,
    'goodbye'   : mac.quit,
    'calibrate' : mac.calibrate,
    'speak_time': mac.speak_time,
    'tell_joke' : mac.tell_joke,
}



